services:
  frontend:
    build: ./frontend
    ports:
      - '8080:8080'
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    build: ./backend
    restart: unless-stopped
    stop_grace_period: 10s
